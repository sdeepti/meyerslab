---

name: srna_libraryinfo
type: query
main_module: services.sRNA_libraryInfo.main.py

whitelist:
  - mpss.danforthcenter.org

icon: ../logo.png

validate_request: true

requirements:
  - requests

endpoints:
  /search:
    summary: List of basic metadata, read counts, etc 
    description: List of basic metadata, read counts, etc
